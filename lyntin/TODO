For a more up-to-date TODO, please check the web-site:
http://lyntin.sourceforge.net

-------------------------------------------------------------
build some kind of sleep command.  so you can sleep some arbitrary amount
between alias commands.

Build a module system.  Then in the user module, folks can add new modules which
all extend the same module class.  This binds it to the system and makes everything
interoperate better.  Also makes adding modules easy to do.  Modules should be
added via #module <blah> where <blah> is in the stdlib.

ctrl-c or something like that should kill the input.  need to think about this.

Replicate more of tintin's functionality.
 - colors


embed lyntin into a C program.
 - portable disabling of echo
 - portable tab completion
 - very fast action parsing

support grepping of _second_ part of actions, aliases?

datagrep{lines} should print results in some order.
datagrep{lines} should have option for case insensitivity

add pattern variables to substitutes

interpreter mode, in which everything typed is sent to a python interpreter.
- avoids needing #@ all the time, also allow multiline stuff

clean up command dispatch

allow multiple actions with the same key (use a setaction method of session
class, stupid)

easy interpolation of python expressions

standard aliases library, maybe actions too

add scheduler as arg to ready_to_perform

fix telnet client

#log input too.

alias/action groups

!!! add put_line to exported

put command_separator in user_custom

