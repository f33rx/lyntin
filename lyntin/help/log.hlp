   syntax: #log <filename>
   Given an argument, starts a log file at <filename> in append mode
   (meaning it won't write over the log file).
   Given no arguments, it stops logging for that session.
