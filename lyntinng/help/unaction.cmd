   syntax: #unaction {pattern}

   Removes actions matching {pattern}.

   ex: #unaction {kill}       <-- removes action 'kill'
   ex: #unaction {*kill*}  <-- removes all actions with 'kill' in them.
