   syntax: #unsubstitute {pattern}

   Removes substitutes matching {pattern}.

   ex: #unsubstitute {kill}   <-- removes substitute 'kill'
   ex: #unsubstitute {*kill*} <-- removes all substitutes with 'kill' 
                                  in them.
